# This is a Declarative Automation Bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: c3c379fd-ec58-439e-906a-c81ad401635a



targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405607475700280.0.azuredatabricks.net
      
    presets:
      source_linked_deployment : false
   
  prod:
    mode: production
    workspace:
      host: https://adb-7405607475700280.0.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/dhiren474dp@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/PROD/.bundle/${bundle.name}/${bundle.target}
  
    permissions:
      - user_name: dhiren474dp@gmail.com
        level: CAN_MANAGE
